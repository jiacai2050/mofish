> <%= data_time %>

__Hacker News__

<%
    const badChars = ['(', ')', '.', '[', ']'];
%>
<%  for(let post of hn_posts) { %>
<%
    let title = post.title;
    for(const c of badChars) {
      title = title.replaceAll(c, ' ');
    }
%>
[<%= title %>](<%- post.url %>) <%= post.score %> points at <%= post.created %> [<%= post.descendants %> comments](https://news.ycombinator.com/item?id=<%= post.id %>)
<%  }  %>

__V2EX__

<%  for(let post of v2ex_posts) { %>
<%
    let title = post.title;
    for(const c of badChars) {
      title = title.replaceAll(c, ' ');
    }
%>
[<%= title %>](<%- post.url %>) in [<%= post.node %>](<%- post.node_url %>)
<%  }  %>
